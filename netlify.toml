[build]
  command = "cd documentation && npm install --legacy-peer-deps && npm run build"
  publish = "documentation/build"

[build.environment]
  NODE_VERSION = "22"
  NPM_CONFIG_LEGACY_PEER_DEPS = "true"
